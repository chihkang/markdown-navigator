// tools/get-tags.ts
/**
 * Get all available tags from project markdown files
 */
export default async function tool() {
  // Implement your tag fetching logic
  // This should return all tags used in the project

  return {
    tags: ["documentation", "notes", "todo", "ideas"],
  };
}
